<%- include('../head'); %> 


  <!-- Contenido -->
  <main class="container pt-5">
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4 g-3 ">
    <% recetas.forEach(function(receta){ %>
      <div class="col">
        <div class="card">
          <!-- <img src="..." class="card-img-top" alt="..."> -->
          <div class="card-body">
            <h5 class="card-title"><%= receta.title %></h5>
            <!-- <a href="/receta?id=<%=receta._id %>" class="btn btn-primary">Más...</a> -->
            <a href="/receta/<%=receta._id %>" class="btn btn-primary">Más...</a>
          </div>
        </div>
      </div>
    <% }); %>
  </div>
  </main>
  <!-- fin contenido -->
  <%- include('../footer'); %> 
